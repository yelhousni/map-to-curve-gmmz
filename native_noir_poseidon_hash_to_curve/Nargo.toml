[package]
name = "native_noir_poseidon_hash_to_curve"
type = "bin"
authors = [""]
compiler_version = ">=0.37.0"
# compiler_version = ">=0.31.0"

[dependencies]
noir_custom_hashes = { path = "../noir_custom_hashes" }