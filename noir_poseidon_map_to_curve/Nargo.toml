[package]
name = "noir_poseidon_map_to_curve"
type = "bin"
authors = [""]
compiler_version = ">=0.37.0"
# compiler_version = ">=0.31.0"

[dependencies]
non_native_field_ops = { path = "../non_native_field_ops" }
noir_custom_hashes = { path = "../noir_custom_hashes" }